# Design QA — 学法图谱与课程内容优化

## Comparison target

- 学法区源图：`/workspace/scratch/9ea159f06747/upload/096d0f22-caae-4089-a877-c93ce40852cc.png`
- 学法区浏览器实现：`/workspace/sites/farun-beijiang/qa/home-learning-map.jpg`
- 行动步骤源图：`/workspace/scratch/9ea159f06747/upload/f96aed74-0763-4661-bb0c-827c78411259.png`
- 行动步骤浏览器实现：`/workspace/sites/farun-beijiang/qa/lesson-action-route.jpg`
- 常见误区源图：`/workspace/scratch/9ea159f06747/upload/619883fc-5d5d-4600-9cd9-c17e4b0f0a24.png`
- 常见误区浏览器实现：`/workspace/sites/farun-beijiang/qa/lesson-myths.jpg`

三组源图与实现截图已分别作为同一组视觉输入进行对照检查。

## Viewport and normalization

- 学法区源图：`2048 × 824`；实现截图：`1363 × 380`。
- 行动步骤源图：`1425 × 499`；实现截图：`1348 × 926`。
- 常见误区源图：`1372 × 332`；实现截图：`1348 × 926`。
- 桌面浏览器 CSS 视口：`1363 × 936`，设备密度 `1×`。
- 手机内容视口：`375px` 宽，设备密度 `1×`；使用站内 QA frame 检查。
- 学法区采用同状态区域对比；课程源图是局部内容截图，因此实现侧保留完整学习页面上下文，重点对比相同的读者内容区域。

## Full-view comparison evidence

- 学法区保留原有深蓝大色块、象牙白正文、朱红章节号与细分隔线；新增图谱后仍维持三段式秩序。中部的“问题”核心节点连接四个真实专题入口，右侧学习步骤改为紧凑纵向列表。
- 行动步骤从四个大面积留白卡片改为一条连续路线。序号、方向箭头和行动文字形成明确的从左到右阅读路径，同时保留原有红蓝极简语言。
- 常见误区从“标题＋说明”的平铺卡片改为上下两层“常见误区 / 正确理解”对照，信息关系更直接，卡片高度也明显收紧。

## Focused comparison evidence

- 学法图谱：检查中心节点、横纵连接线、四个专题图标、入口箭头和右侧方法步骤的对齐。
- 行动路线：检查四个等宽步骤、边界线、箭头跨栏位置及长文本换行。
- 误区卡片：检查红色错误标签、蓝色正确标签、上下分区边界和双列对齐。
- 三个关键区域均在浏览器渲染截图中清晰可读，因此无需额外放大裁切。

## Required fidelity surfaces

- Fonts and typography：沿用既有粗黑中文标题、窄体英文章节标签和高字重导航；新增图谱与卡片标签没有引入新的字体风格。长步骤只在卡片内部自然换行，不发生截断。
- Spacing and layout rhythm：学法区由两列升级为三段式，图谱占据主要视觉宽度；行动步骤与误区卡片均减少无效留白。桌面端分栏规则连续，手机端按单列顺序展开。
- Colors and visual tokens：仅使用现有象牙白、深蓝、蓝色与朱红色变量；没有渐变或新增阴影体系。错误与正确状态分别使用朱红和蓝色，语义清楚。
- Image quality and asset fidelity：本次三处均为界面信息组件，不涉及替换图片资产；图标继续使用项目现有 Phosphor 图标库，不使用手绘 SVG、Emoji 或占位符。
- Copy and content：主页仍保持短文案。新增“法律知识图谱”只显示四个专题名称；详细知识仍在点击后进入分层学习页。误区与步骤的法律内容未删减。

## Primary interactions tested

- 从主页“法律知识图谱”点击“进入反诈防骗”，正确打开对应学习专题。
- 切换“行动步骤”，确认四步路线与全部行动文案显示。
- 切换“常见误区”，确认两张卡片均显示“常见误区 / 正确理解”双层结构。
- 桌面端确认页面无水平溢出。
- 手机端确认 `375px` 视口无水平溢出；学法图谱宽 `335px`，四个行动步骤各宽 `335px` 并纵向排列，两张误区卡片均为 `335px` 宽。
- 构建、Sites Worker 测试与浏览器渲染均通过。

## Console check

- 新开的本地预览标签页没有应用来源的 warning 或 error。
- 仅观察到 `chrome-extension://` 浏览器元数据消息，属于外部扩展，不计入站点错误。

## Findings

- 无可执行的 P0、P1 或 P2 问题。
- 学法区高度比原版更紧凑，是为了容纳图谱同时保持首页节奏，属于本轮明确的密度优化。
- 行动路线和误区对照不再复刻原先的大卡片留白，是用户要求的美化与信息关系强化。

## Comparison history

- Pass 1：三组源图与浏览器实现完成同组对照。未发现 P0/P1/P2 差异，因此无需视觉修复循环。

## Implementation checklist

- [x] 四专题法律知识图谱可点击。
- [x] 行动步骤形成连续路线。
- [x] 常见误区形成错误与正确的直接对照。
- [x] 桌面与手机布局无水平溢出。
- [x] 浏览器控制台无应用错误。

final result: passed
